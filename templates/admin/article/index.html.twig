{% extends 'base.html.twig' %}

{% block title %}Article index{% endblock %}

{% block content %}
    <h1>Article index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Image</th>
                <th>Créer le</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.id }}</td>
                <td>{{ article.title }}</td>
                <td>{{ article.image }}</td>
                
                <td>{{ article.createdAt ? article.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('admin_article_show', {'id': article.id}) }}">voir</a>
                    <a href="{{ path('admin_article_edit', {'id': article.id}) }}">éditer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        
        </tbody>
    </table>
<p> Action </p>
    <a href="{{ path('admin_article_new') }}">Ajouter une pop</a>
    </br>
    </br>
    <a href="{{ path('home') }}">home</a>
    
{% endblock %}
